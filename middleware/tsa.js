export default function({route, store, redirect, error}){
    // if(route.path === '/login'){
    //     error({
    //         statusCode: 403,
    //         message: 'Test'
    //     });
    // }
}